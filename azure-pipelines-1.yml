# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

resources:
  containers:
  - container: ni-smartracks-dev
    image: rnd-builds.repos.natinst.com/smartracks/smartracks:10
    options:

jobs:
- job: Build_OpenEmbedded
  container: ni-smartracks-dev
  timeoutInMinutes: 3600
  pool:
    name: Drivers-NIBuildFarm-RFMIBUILD
    demands:
      - docker
      - Agent.OS -equals Linux
  steps:
  - checkout: self
    submodules: true
  - script: |
      echo "Hello World"
